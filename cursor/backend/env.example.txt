# Django Environment Configuration
# Copy this file to .env and update the values for your setup

# Django Settings
SECRET_KEY=change-this-to-a-secure-random-key-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# MySQL Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=best_in_solutions
DB_PORT=3306

# CORS Settings (Frontend URL)
# For development:
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
# For production:
# CORS_ALLOWED_ORIGINS=https://your-domain.com

# WebSocket Configuration (Optional)
# For development, uses InMemoryChannelLayer (no Redis needed)
# For production, configure Redis:
# CHANNEL_REDIS_URL=redis://localhost:6379/0
